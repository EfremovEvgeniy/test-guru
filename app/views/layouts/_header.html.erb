<% if user_signed_in? %>
  <p class="nav user"><%= t('.welcome', user_email: current_user.email) %> Guru |
    <%= link_to t('.logout'), destroy_user_session_path, method: :delete %> | <%= link_to "TestGuru", root_path %></p>
<% end %>
